# Ansible Inventory for Health Dashboard
# Replace the IP address with your actual server IP (from Terraform output)

[webservers]
health-dashboard ansible_host=YOUR_SERVER_IP ansible_user=ec2-user ansible_ssh_private_key_file=../terraform/my-devops-key.pem

[webservers:vars]
ansible_python_interpreter=/usr/bin/python3
